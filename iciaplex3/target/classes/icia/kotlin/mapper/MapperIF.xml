<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" 
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="icia.kotlin.mapper.MapperIF">
	<select id="getDate()" resultType="Int">
		SELECT SYSDATE FROM DUAL
	</select>
	<select id="isMember" resultType="Int">
		SELECT count(*) FROM ST WHERE ST_ID = #{mId}
	</select>
	<select id="isAccess" resultType="Int">
		SELECT count(*) FROM ST WHERE ST_ID= #{mId} and ST_PASSWORD= #{mPwd}
	</select>
	<select id="isMInfo" resultType="icia.kotlin.beans.Member">
		SELECT ST_ID AS mId, ST_NAME AS mNAME, ST_PASSWORD AS mPwd, ST_PHONE AS mPhone FROM ST WHERE ST_ID= #{mId} and ST_PASSWORD= #{mPwd}
	</select>
	<insert id="insCustomer" >
		INSERT INTO ST (ST_ID,ST_NAME,ST_PASSWORD,ST_PHONE) VALUES(#{mId},#{mName},#{mPwd},#{mPhone})
	</insert>
	<insert id="insMovie" >
		INSERT INTO MV (MV_CODE, MV_NAME, MV_GRADE, MV_STATUS, MV_IMAGE, MV_COMMENTS) VALUES(#{mvCode},#{mvName},#{mvGrade},#{mvStatus},#{mvImage},#{mvComment})
	</insert>
	
</mapper>
